{
  "name": "meus-scripts",
  "version": "1.0.0",
  "description": "",
  "main": "setup.js",
  "bin": {
    "meucomando": "./setup.js"
  },
  "scripts": {
    "start": "node setup.js",
    "dev": "npm run build-css && npm run watch-css",
    "build-css": "sass src/scss:dist/css --no-source-map",
    "watch-css": "sass --watch src/scss:dist/css --no-source-map & postcss dist/css/*.css --use autoprefixer -d dist/css/"
  },
  "dependencies": {},
  "devDependencies": {
    "sass": "^1.32.0",
    "postcss-cli": "^8.3.1",
    "autoprefixer": "^10.2.4"
  }
}
